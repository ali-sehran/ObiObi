# ObiObi
帯 A quiet band around your shell. Ask in plain English, the command appears in dim grey, Tab accepts it. Nothing runs until you do. Local model or any OpenAI-compatible endpoint. Risky commands need two Tabs. One dependency.
